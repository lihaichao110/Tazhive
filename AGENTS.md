# Tazhive 编码代理约束

## 沟通与交付

- 回答、说明、提交信息和项目文档默认使用中文；代码标识符保持英文。
- 修改前先确认模块职责和依赖方向，禁止为了完成局部需求破坏现有分层。
- 交付前必须执行 `pnpm check` 与 `pnpm build`，并说明未执行或失败的检查。

## 文件与拆分

- 源码、样式、配置和项目文档单文件最多 300 行；生成文件和锁文件除外。
- 该限制不交给 Lint 阻断。任何修改导致文件超限时，编码代理必须主动完成拆分后再交付。
- 文件达到 200–250 行时优先评估拆分，顺序为：子组件、Hook、类型、常量、静态数据、工具函数和样式。
- 一个文件只承担一个主要职责；一个主要组件或 Hook 对应一个文件。
- 禁止用压缩格式、超长行或删除必要说明规避行数限制。

## 架构边界

- 依赖方向为 `app → pages → widgets/features → entities → shared`，下层不得导入上层。
- `features`、`widgets`、`entities` 和 `pages` 的外部消费者只能通过模块根部 `index.ts` 导入。
- 禁止跨模块深层导入；模块内部允许使用相对路径。
- 页面负责组合，feature 负责业务能力，entity 负责领域模型，shared 只承载无业务含义的复用能力。
- 全局 Provider、路由和 Store 只能在 `src/app` 初始化。

## TypeScript 与 React

- 组件使用 PascalCase，Hook 使用 `useXxx`，变量和函数使用 camelCase，常量使用 UPPER_SNAKE_CASE。
- 优先具名导出；默认导出仅用于确有框架约定的应用或页面入口。
- 类型导入使用 `import type`，Props 使用具名只读类型，公共函数明确返回类型。
- 禁止无理由的 `any`、非空断言、双重类型断言和忽略 TypeScript 错误。
- 副作用、请求和可复用状态进入 Hook 或业务层；展示组件只接收数据并渲染。
- 不在 JSX 内堆积复杂计算、匿名大对象或多层条件分支，应提取为具名逻辑。

## Props 与状态边界

- 业务数据、状态和回调只能通过 Props 传给直接消费它们的组件。
- 组件若不读取、转换或执行某个业务 Prop，只将其原样传给后代，即属于禁止的纯透传。
- 发现纯透传时，依次考虑调整组件职责、使用组件组合、移动状态归属，或建立 feature/page 作用域 Context。
- 禁止仅为消除 Props 使用全局 Store、事件总线，或把业务状态移动到 `shared`。
- Context 必须围绕同一业务能力组织并限定在最小有效子树；只有跨页面、跨业务生命周期的状态才能进入 `app/store`。
- `children`、第三方组件插槽和明确的协议适配参数不属于业务 Prop 透传，但适配层不得继续跨多个自有组件传递业务参数。
- 修改或评审组件时，必须检查新增 Props 是否形成纯透传链路。

## 文件内部顺序

1. 第三方导入。
2. 当前模块内部相对导入。
3. 使用 `@/` 的跨模块导入。
4. 类型声明。
5. 常量与静态配置。
6. 主要组件、Hook 或函数。

复杂辅助逻辑不得长期堆放在主文件末尾，应根据职责移入 `model`、`lib`、`hooks` 或子组件。

## 样式、质量与注释

- 组件样式使用 CSS Modules；全局设计令牌、重置和基础样式只放在 `src/app/styles`。
- 颜色、间距和排版优先复用设计令牌，禁止随意污染全局选择器。
- 交互元素必须支持键盘操作、清晰焦点和必要的无障碍名称。
- 数据驱动界面必须考虑加载、空数据、失败和成功状态。
- 主要组件、Hook、公共函数及承担核心职责的内部函数，应在定义前使用简短中文注释说明职责、适用边界或关键输入输出。
- 异步流程、状态同步、生命周期清理、复杂算法、坐标换算、协议适配、安全处理及兼容性取舍，必须在关键位置说明设计原因和不变量。
- 注释不得逐行翻译代码；简单类型、显而易见的条件判断、展示 JSX 和普通访问器无需强制添加。
- 修改逻辑时必须同步维护相关注释，禁止保留失效或误导性说明。
